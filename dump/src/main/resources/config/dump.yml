# controls how to dump entire request and response to log file. not recommended
# on production if performance is important
enabled: ${dump.enabled:false}
mask: ${dump.mask:false}
# logger level ERROR | WARN | INFO | DEBUG
logLevel: ${dump.logLevel:INFO}
# indent size for logging info
indentSize: ${dump.indentSize:4}
# use json style or not, if use json, indentSize option will be useless
useJson: ${dump.useJson:false}
requestEnabled: ${dump.requestEnabled:false}
# if "request: false", disable dumping requests.
#request:
#  url: true
#  headers: true
#  # filter for headers
#  filteredHeaders:
#  - Postman-Token
#  - X-Correlation-Id
#  - cookie
#  cookies: true
#  #filter for cookies
#  filteredCookies:
#  - Cookie_Gmail
#  queryParameters: true
#  #filter for queryParameters
#  filteredQueryParameters:
#  - itemId
#  - a
#  body: true
request: ${dump.request:}
#response:
#  headers: true
#  cookies: true
#  body: true
#  statusCode: true
responseEnabled: ${dump.responseEnabled:false}
response: ${dump.response:}
